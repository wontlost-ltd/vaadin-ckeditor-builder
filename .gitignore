# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.project
.classpath
.settings/

# Vaadin
node_modules/
frontend/generated/
vite.generated.ts
# vite.config.ts 由 vaadinPrepareFrontend 脚手架生成（内容为包装 vite.generated.ts 的固定模板），
# 与同目录的 tsconfig.json / package.json / types.d.ts 同属生成物，不纳入版本控制。
vite.config.ts
types.d.ts
tsconfig.json
pnpm-lock.yaml
package-lock.json
package.json

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Environment variables (contains secrets)
.env
.env.local
.env.*.local

# Oracle Wallet (contains sensitive connection info)
wallet/
*.wallet/
*_wallet/

# Build artifacts
target/
out/
bin/

# Test reports
test-output/
.ace-tool/

# Claude AI work files
.claude/

# CCG 工作流的本地任务状态与验证证据（task.json、上下文、浏览器截图等），
# 属流程元数据而非交付物，不纳入版本控制。
.ccg/

# Local Maven repository
.m2/

# H2 file database
data/

# Vaadin bundles (large build artifacts)
src/main/bundles/dev.bundle
src/main/bundles/prod.bundle
